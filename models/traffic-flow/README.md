# Traffic Flow Fundamental Diagrams

Compares three classic speed-density relationships.

```text
Greenshields: v = vf * (1 - k / kj)
Greenberg:    v = c * ln(kj / k)
Underwood:    v = vf * exp(-k / k0)
q = k * v
```

The app plots both speed-density and flow-density curves.
